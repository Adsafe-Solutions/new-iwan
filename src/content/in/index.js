/* India overrides. Base was written for India, so there is nothing to override
   yet — list only the keys that differ, and everything else is inherited.

   Arrays replace rather than merge (see ../merge.js), so an override of
   `events` must be the country's complete list. */
export default {};
