/* "Iwan by the numbers" — the counters on the homepage.

   `count` is animated up from zero by the [data-count] handler in
   useGsap.js; `suffix` is appended after the formatted number. */
export const STATS = [
  { count: 60, label: "events hosted" },
  { count: 20, label: "speakers so far" },
  { count: 4, label: "iwan projects" },
  { count: 2000, label: "people involved" },
];

export default STATS;
