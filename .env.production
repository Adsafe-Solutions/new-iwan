# `npm run build` and `npm run build:prod`. No secrets — see  .env.example.
VITE_ENVIRONMENT=prod
# ⚠ new. for now, not www. — www.iwan.community is the live WordPress site.
# Change this to https://www.iwan.community at cutover, alongside the route
# in wrangler.toml.
VITE_SITE_URL=https://new.iwan.community
VITE_DEFAULT_COUNTRY=in
